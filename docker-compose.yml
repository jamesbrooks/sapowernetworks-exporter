# Docker Compose configuration for SA Power Networks Exporter
services:
  sapowernetworks-exporter:
    build: .
    image: jamesbrooks/sapowernetworks-exporter
    ports:
      - "9120:9120"
    env_file:
      - .env
    restart: unless-stopped
