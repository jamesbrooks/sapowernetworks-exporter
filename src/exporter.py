"""Prometheus metrics exporter module.

This module handles:
- Defining Prometheus metrics (gauges, counters)
- Exposing metrics HTTP server on port 9120
- Updating metrics with parsed NEM12 data
"""
